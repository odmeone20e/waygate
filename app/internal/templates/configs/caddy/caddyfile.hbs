{
    layer4 {
        {{#each (split WGConfig.Interface.DNS ",")}}
        # 0.0.0.0:<port> {
        #    route {
        #        proxy {
                    # proxy service upstreams here, e.g.
                    # upstream udp/10.0.0.3:10123
                    # more: https://github.com/mholt/caddy-l4
        #        }
        #    }
        #}
        {{/each}}
    }
}