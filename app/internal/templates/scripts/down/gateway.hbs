docker exec {{ waygateGatewayContainerName }} sh -c 'for svc in /etc/service/*; do sv down "$svc"; done'  && \
docker exec {{ waygateGatewayContainerName }} waygate gateway down && \
docker exec {{ waygateGatewayContainerName }} sh -c 'rm -rf /app/waygate/*' && \
docker stop {{ waygateGatewayContainerName }} && \
docker rm {{ waygateGatewayContainerName }} && \
docker rmi {{ waygateGatewayContainerImage }} || true
